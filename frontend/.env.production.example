# Mismo dominio que sirve /api (recomendado): déjalo vacío.
VITE_API_URL=

# Si la API está en otro host (ej. api.tudominio.com):
# VITE_API_URL=https://api.tudominio.com

# Checkout Hotmart (Plan Pro). Mismo producto que dispara el webhook.
# VITE_HOTMART_CHECKOUT_URL=https://pay.hotmart.com/TU_PRODUCTO
